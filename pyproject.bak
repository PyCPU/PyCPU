[tool.poetry]
name = "PyCPU"
version = "0.7.0"
description = "Virtual CPU for Python"
authors = ["Scott McCallum (https github.com scott91e1 CV)"]

[tool.poetry.dependencies]
python = "^3.6"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
